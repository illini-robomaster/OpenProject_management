# Production Environment Variables
# Copy this to .env.prod and customize for your production environment

# OpenProject Version
TAG=16-slim

# Domain Configuration - CHANGE THIS!
DOMAIN_NAME=your-domain.com
HTTP_PORT=80
HTTPS_PORT=443

# SSL/TLS Configuration
OPENPROJECT_HTTPS=true
OPENPROJECT_HSTS=true
OPENPROJECT_FORCE_SSL=true

# Database Configuration - CHANGE THIS PASSWORD!
POSTGRES_PASSWORD=your-super-secure-database-password-here

# Email Configuration - CONFIGURE FOR PRODUCTION
# OPENPROJECT_SMTP_ADDRESS=smtp.your-provider.com
# OPENPROJECT_SMTP_PORT=587
# OPENPROJECT_SMTP_DOMAIN=your-domain.com
# OPENPROJECT_SMTP_USER_NAME=your-email@your-domain.com
# OPENPROJECT_SMTP_PASSWORD=your-email-password
# OPENPROJECT_MAIL_FROM=openproject@your-domain.com

# Data Persistence
PGDATA=pgdata
OPDATA=opdata

# Application Configuration
OPENPROJECT_RAILS__RELATIVE__URL__ROOT=
IMAP_ENABLED=false
DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@db/openproject?pool=20&encoding=unicode&reconnect=true
RAILS_MIN_THREADS=4
RAILS_MAX_THREADS=16

# Security Settings
OPENPROJECT_SESSION_STORE=cache_store
OPENPROJECT_SESSION_TTL=120

# Backup Configuration (uncomment and configure as needed)
# BACKUP_SCHEDULE="0 2 * * *"  # Daily at 2 AM
# BACKUP_RETENTION_DAYS=7
